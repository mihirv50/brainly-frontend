import { useState } from "react"

const CreateContentModal = () => {
    const [modalOpen, setModalOpen] = useState(false);
  return (
    <div>CreateContentModal</div>
  )
}

export default CreateContentModal